$.fn.typewriter = function() {
	

	// Sets user selected element to variable (???)
	var chosenElement = $(this).split('');

	// Convert selected element (text) to array
	console.log(chosenElement);

	});
}

// Create For Loop that will run the function of making text appear one letter at a time

// For Loop will continue running until last letter is reached

// Potential customization options
//typing speed, cursor showing, consistency of typing